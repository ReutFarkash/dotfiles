
if [ "$DEBUG" == true ]; then
    scriptpath="${CUR_DIR}/${BASH_ARGV[0]}"
    echo "running $scriptpath"
fi

export PROJECT_PATH="${HOME}/Code/my-project/"

# ── Machine/user-specific tool aliases ────────────────────────────────────────
# Uncomment and adapt as needed. These belong here, not in .bash_aliases,
# because they reference paths that differ per user or machine.

# Gemini CLI with project-specific include directories
# alias my_gemini="gemini --include-directories $HOME/Code/_shared-gemini,$HOME/Code/gemini-skills"

# Scout multi-agent orchestrator
# alias scout="$HOME/Code/multi-agent-orchestrator/scripts/scout.sh"

# Code base root (used by aliases that need $CODE_BASE)
# export CODE_BASE="$HOME/Code"
